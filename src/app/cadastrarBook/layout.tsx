import Link from "next/link";

export default function cadastrarBookLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return (
      <>
      
     
      {children}
     </>
    );
  }
  